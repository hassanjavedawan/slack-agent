{
	"name": "viktor-spaces-app",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"sync": "npx convex deploy --cmd 'npx vite build'",
		"sync:build": "npx convex deploy --cmd 'npx vite build'",
		"check": "npx @biomejs/biome check .",
		"test": "bun run scripts/test.ts",
		"screenshot": "bun run scripts/screenshot.ts",
		"logs:fetch": "npx convex logs"
	},
	"dependencies": {
		"convex": "^1.17.0",
		"react": "^19.0.0",
		"react-dom": "^19.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/react": "^19.0.0",
		"@types/react-dom": "^19.0.0",
		"typescript": "^5.7.0",
		"vite": "^6.0.0",
		"@vitejs/plugin-react": "^4.3.0"
	}
}
