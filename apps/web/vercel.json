{
	"buildCommand": "cd ../.. && bun install && bun run --filter @openviktor/web build",
	"outputDirectory": "dist",
	"rewrites": [
		{ "source": "/api/(.*)", "destination": "/api/proxy" },
		{ "source": "/((?!api/).*)", "destination": "/index.html" }
	]
}
